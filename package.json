{
    "name": "alpen",
    "version": "1.0.0",
    "private": true,
    "description": "",
    "scripts": {
        "prebuild": "pnpm -r --parallel typecheck",
        "build": "pnpm -r --parallel build",
        "lint": "eslint .",
        "lint:fix": "eslint --fix ."
    },
    "devDependencies": {
        "@types/node": "^22.7.4",
        "esbuild": "^0.24.0",
        "eslint": "^9.12.0",
        "typescript-eslint": "^8.8.1",
        "@eslint/js": "^9.12.0"
    }
}
