{
    "name": "alpen",
    "version": "1.0.0",
    "private": true,
    "description": "",
    "scripts": {
        "prebuild": "pnpm -r --parallel typecheck",
        "build": "pnpm -r --parallel build",
        "lint": "oxlint -c .oxlintrc.json --import-plugin",
        "lint:fix": "oxlint -c .oxlintrc.json --import-plugin --fix"
    },
    "devDependencies": {
        "@types/node": "^22.7.4",
        "esbuild": "^0.24.0",
        "oxlint": "^0.9.9"
    }
}
